<%= form_for(@temp_address) do |f| %>
  <% if @temp_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
      <% @temp_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_field :street %>
  </div>

  <div class="field">
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.text_field :zip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

  <%= link_to "Search", search_results_path(:street, :city, :state, :zip)%>
<% end %>
